console.log(Math.round(1.6)); //반올림 함수
console.log(Math.round(1.4)); //반올림 함수, 입력값이 있고 출력값이 있다.

function sum(first, second){ //parameter 매개변수
    console.log('a');
    return first + second;
}

console.log(sum(2, 4)); //각각의 입력값 하나하나를 argument

